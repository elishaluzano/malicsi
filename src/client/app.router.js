(function() {
    angular
        .module('app')
        .config(routerConfig);
    
    function routerConfig($stateProvider, $urlRouterProvider) {

        // $stateProvider.state({});
        // $urlRouterProvider.otherwise('/');
    }

})();
